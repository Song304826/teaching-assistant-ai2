# 课研助手V4：从解压到分享给评委

这份教程按第一次接触项目开发的同学来写。请从第一步开始，不要跳步。

## 第一部分：在自己的Windows电脑运行

### 第1步：准备软件

需要安装：

1. Python 3.11；安装界面务必勾选 `Add Python to PATH`。
2. Visual Studio Code。
3. 谷歌Chrome或Microsoft Edge浏览器。

安装后按 `Win + R`，输入 `powershell`，执行：

```powershell
py -3.11 --version
```

显示 `Python 3.11.x` 才能继续。

### 第2步：解压项目

把压缩包解压到桌面。不要直接在压缩包里打开代码。建议文件夹名称保持：

```text
teaching_assistant_final_v4
```

### 第3步：使用VS Code打开正确文件夹

1. 打开VS Code。
2. 点击“文件”。
3. 点击“打开文件夹”。
4. 选择 `teaching_assistant_final_v4`。
5. 左侧必须能直接看到 `app.py`、`requirements.txt`、`modules`、`knowledge_base`。

如果左侧还多包了一层同名文件夹，说明打开位置不正确。

### 第4步：配置DeepSeek密钥

1. 左侧找到 `.env.example`。
2. 复制一份。
3. 把复制文件改名为 `.env`。
4. 打开 `.env`。
5. 把第一行改成自己的真实密钥：

```text
DEEPSEEK_API_KEY=sk-xxxxxxxx
```

不要删除另外两行，不要在等号两边添加空格。

### 第5步：创建虚拟环境

在VS Code顶部选择“终端—新建终端”，确认终端路径最后是项目文件夹，然后执行：

```powershell
py -3.11 -m venv .venv
```

不需要运行 `Activate.ps1`，所以不会受到PowerShell脚本策略影响。

### 第6步：安装基础依赖

```powershell
.\.venv\Scripts\python.exe -m pip install -r .\requirements.txt
```

看到红色报错时不要继续启动，把完整错误截图保存。只有命令正常结束后才能继续。

### 第7步：启动网页

```powershell
.\.venv\Scripts\python.exe -m streamlit run .\app.py
```

浏览器通常会自动打开；没有打开时手动访问：

```text
http://localhost:8501
```

终端必须保持打开。按 `Ctrl + C` 可以关闭网页服务器。

## 第二部分：安装Paraformer语音转文字

先确认文字访谈和课件生成能够正常运行，再安装语音依赖：

```powershell
.\.venv\Scripts\python.exe -m pip install -r .\requirements-voice.txt
```

安装完成后停止并重新启动网页。第一次点击“将录音转换为文字”时会下载Paraformer模型，请保持网络稳定并耐心等待。以后会使用本机缓存。

如果电脑内存不足或模型下载失败，不影响文字输入、知识库和课件生成。

## 第三部分：完整测试流程

1. 第一页输入：“我要给七年级学生讲一元一次方程的移项法则，人教版，45分钟。”
2. 回答AI后续问题。
3. 选择“先例题后总结理论”、18页、丰富、科技蓝。
4. 跨学科选择“语文”和“数学史”，呈现方式选择“故事或古文导入”。
5. 进入知识库，搜索“移项法则易错点和课堂互动”。
6. 点击“确认资料并生成”。
7. 点击“生成第一版教学方案”，观察进度到100%。
8. 查看PPT大纲、教案、融合内容和讲义。
9. 进入修改页面，输入：“增加一道容易忘记变号的例题，并精简第5页。”
10. 下载PPT、教案、教师讲义和学生学习单，逐个打开检查。
11. 打开课堂互动中心，完成AI即时小测。

## 第四部分：上传GitHub

### 第1步：新建仓库

1. 登录GitHub。
2. 点击右上角 `+`。
3. 点击 `New repository`。
4. Repository name填写 `teaching-assistant-ai`。
5. 选择Public或Private均可；使用免费Streamlit部署时需确认当前账户支持相应仓库。
6. 点击 `Create repository`。

### 第2步：上传项目

最重要的要求是保留文件夹结构。GitHub仓库首页应直接看到：

```text
app.py
requirements.txt
modules/
knowledge_base/
games/
.streamlit/
```

不要把 `modules` 里面的Python文件全部放到仓库根目录，否则会再次出现：

```text
ModuleNotFoundError: No module named 'modules'
```

禁止上传：

```text
.env
.venv
真实API密钥
```

`.env.example` 可以上传，因为里面没有真实密钥。

## 第五部分：部署为评委可以打开的网站

1. 打开 Streamlit Community Cloud。
2. 使用GitHub账号登录。
3. 点击 `Create app` 或 `New app`。
4. Repository选择 `teaching-assistant-ai`。
5. Branch选择 `main`。
6. Main file path填写 `app.py`。
7. 打开Advanced settings或应用设置中的Secrets。
8. 填写：

```toml
DEEPSEEK_API_KEY = "sk-你的真实密钥"
DEEPSEEK_BASE_URL = "https://api.deepseek.com"
DEEPSEEK_MODEL = "deepseek-chat"
```

9. 点击Deploy。
10. 等待安装完成，得到类似下面的网址：

```text
https://teaching-assistant-ai.streamlit.app
```

把这个网址发给队友和评委即可。评委不需要安装Python，也不需要填写密钥。

注意：免费云端通常不适合加载完整Paraformer。比赛公网版应优先保证文字输入、知识库、PPT和教案生成稳定；语音功能在本机演示视频中完整展示。

## 第六部分：以后增加知识库

1. 在 `knowledge_base/documents` 中按学科创建文件夹。
2. 把新资料放进去。
3. 本地打开网页，进入“资料与知识库”。
4. 点击“同步团队知识库”。
5. 检查新增、更新和失败数量。
6. 把新资料上传到同一个GitHub仓库。
7. Streamlit会自动重新部署，原来的网址不变。

不需要重新创建仓库，也不需要重新创建Streamlit应用。

## 第七部分：以后加入队友的小游戏

1. 让队友提供完整游戏文件夹，而不是只发一段代码截图。
2. 游戏应提供 `game.py` 和 `game_config.json`。
3. `game.py` 必须包含 `render_game(game_data)`。
4. 把游戏文件夹放入 `games`。
5. 将配置中的 `enabled` 改为 `true`。
6. 本地测试游戏。
7. 上传到原GitHub仓库。
8. 原公网地址自动更新。

队友如果使用Pygame，先把完整代码交给项目负责人转换为Streamlit或HTML网页游戏，不能直接把桌面窗口放入Streamlit Cloud。

## 第八部分：比赛提交前

最终至少准备：

- 公网体验地址；
- 项目演示视频；
- 项目介绍PPT；
- 项目详细方案；
- 本地知识库资料；
- 源代码压缩包；
- 本操作说明；
- 一套提前生成并验证过的示例PPT和Word成果。

比赛前一天，不要再大范围改页面。使用另外一台电脑和手机分别打开公网链接，完整走一遍生成和下载流程。
