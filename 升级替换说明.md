# 从旧版本升级到V4

为了避免旧文件位置错误或遗漏模块，建议把V4作为一个全新文件夹使用，不要只复制 `app.py`。

如果需要保留旧版本中的团队资料，只复制下面两类内容：

1. 经过审核的知识库文件：放入 `knowledge_base/documents/对应学科/`；
2. 队友完成的小游戏文件夹：按 `games/README.md` 的接口整理后放入 `games/`。

不要复制旧版本的 `.venv`、`.env`、`__pycache__` 或根目录中的零散模块文件。

首次启动按 `零基础部署与使用教程.md` 操作。公网部署时应上传整个V4目录的内部结构，并在Streamlit Secrets中重新配置DeepSeek密钥。
